# Use Node.js version 20.x
node-version=20.x

# Ensure HTTPS is used for package registry
registry=https://registry.npmjs.org/

# Increase network timeout to handle large packages
network-timeout=100000

# Disable audit for faster installations
audit=false

# Allow installing packages with security vulnerabilities (temporary for build)
force=true 